package main

const (
	ADD              string = "add"
	UPDATE           string = "update"
	DELETE           string = "delete"
	LIST             string = "list"
	MARK_TO_DO       string = "mark-to-do"
	MARK_IN_PROGRESS string = "mark-in-progress"
	MARK_DONE        string = "mark-done"
)

const DB_FILE = "task.json"
